/** Keep the picker filter and every user-facing extension list in sync here. */
export const SUPPORTED_TRACK_DIALOG_EXTENSIONS = ['bw', 'bigWig', 'bedGraph', 'tdf', 'bam', 'bai', 'csi', 'bed'] as const
export const SUPPORTED_TRACK_EXTENSION_LABEL = '.bw, .bigWig, .bedGraph, .tdf, .bam + .bai/.csi, .bed'
